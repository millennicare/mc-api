# Test Environment Variables
# These are mock values used only for testing

# Base URL
BASE_URL=http://localhost:3000

# Database (using SQLite for tests)
DATABASE_URL=sqlite+aiosqlite:///:memory:

# Email Settings (mock values)
RESEND_API_KEY=test_resend_api_key_12345

# Storage Settings (mock Cloudflare values)
CLOUDFLARE_ACCOUNT_ID=test_account_id
CLOUDFLARE_SECRET_ACCESS_KEY=test_secret_access_key
CLOUDFLARE_ACCESS_KEY_ID=test_access_key_id
CLOUDFLARE_BUCKET_NAME=test_bucket

# Maps Settings (mock Google Maps API key)
GOOGLE_MAPS_API_KEY=test_google_maps_api_key

# JWT Settings (mock secret key for testing)
SECRET_KEY=test_secret_key_for_testing_only_do_not_use_in_production
